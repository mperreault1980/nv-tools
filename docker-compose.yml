version: '3.8'
services:
app:
build: .
ports:
- "8080:8080"
env_file:
- .env
networks:
- internal
db:
image: postgres:15
Shell
environment:
POSTGRES_DB: devdb
POSTGRES_USER: devuser
POSTGRES_PASSWORD: changeme
networks:
- internal
networks:
internal:
ipam:
config:
- subnet: 10.42.0.0/16
